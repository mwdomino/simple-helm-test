apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "simple-helm-test.fullname" . }}-test-connection"
  labels:
    {{- include "simple-helm-test.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "simple-helm-test.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
